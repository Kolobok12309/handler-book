{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "dependsOn": ["clean"],
      "inputs": [
        "src/**",
        "build.config.ts",
        "tsconfig.json"
      ],
      "outputs": ["dist/**"]
    }
  }
}
